# MediRely — WSL2 GENUINE NVIDIA cuVS environment (Ubuntu, conda: medirely_live).
# Versions below match the maintainer's TESTED WSL2 environment. Install a CUDA-12
# PyTorch and genuine cuVS SEPARATELY first (they use special indexes):
#
#   pip install torch==2.8.* torchvision==0.23.* --index-url https://download.pytorch.org/whl/cu129
#   pip install --extra-index-url https://pypi.nvidia.com cuvs-cu12==24.12.*
#
# Then install the pinned application dependencies:
transformers==4.45.2
tokenizers>=0.20
safetensors>=0.4
huggingface_hub==0.25.2
torchxrayvision>=1.2
scikit-learn>=1.1
pillow>=9.0
numpy>=1.23
pandas>=1.5
pyyaml>=6.0
gradio==4.44.1
gradio_client==1.3.0
fastapi==0.112.2
starlette==0.38.6
pydantic==2.9.2
uvicorn>=0.30
